from tkinter import *

def hello():
    label = Label(one,text='Hello World').pack()

one = Tk()
one.geometry('500x500+500+300')
one.title('My firth projecct')

label = Label(one,text='mylabel').pack()
button = Button(one,text='OK',command = hello,fg='red',bg='black' ).pack()
